kind: "CDN"
version: "1"
metadata:
  envTypes: ["stage"]
data:
  authentication:
    authenticators:
      - name: purge-auth
        type: purge
        purgeKey1: ${{CDN_PURGEKEY_STAGE_1}}
        purgeKey2: ${{CDN_PURGEKEY_STAGE_2}}
    rules:
      - name: purge-auth-rule
        when:
          allOf:
            # Changed 'equals' to 'in' to support both tiers
            - reqProperty: tier
              in: ["publish", "preview"]
            - reqProperty: forwardedDomain
              in:
                # Publish Domains
                - "stage-publish.marriottvacationclubs.com"
                - "stage-publish.docs.marriottvacationclubs.com"
                - "stage-publish.docs.hyattvacationclub.com"
                # Preview Domains
                - "stage-preview.marriottvacationclubs.com"
                - "stage-preview.docs.marriottvacationclubs.com"
                - "stage-preview.docs.hyattvacationclub.com"
        action:
          type: authenticate
          authenticator: purge-auth
