{
  "smtp.host": "$[env:AEM_PROXY_HOST;default=proxy.tunnel]",
  "smtp.port": "$[env:SMTP_PORT;type=Integer;default=30587]",
  "smtp.password": "value not used",
  "from.address": "$[env:SMTP_FROM_ADDRESS]",
  "smtp.ssl": false,
  "smtp.starttls": true,
  "smtp.requiretls": true,
  "debug.email": true,
  "oauth.flow": true
}
